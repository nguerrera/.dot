# i3status configuration file.
# see "man i3status" for documentation.

general {
        colors = true
        interval = 5
}

order += "battery all"
order += "tztime local"

battery all {
        format = "%status %percentage"
}

tztime local {
        format = "%Y-%m-%d %H:%M:%S"
}
